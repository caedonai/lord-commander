#!/bin/sh
# Pre-commit hook for Lord Commander SDK

echo "🔍 Running pre-commit checks..."

# Run linting and formatting
echo "📏 Running linting and formatting with Ultracite..."
pnpm lint:check || exit 1

# Run tests
echo "🧪 Running tests..."
pnpm test || exit 1

# Build check
echo "🔨 Testing build..."
pnpm build || exit 1

echo "✅ Pre-commit checks passed!"