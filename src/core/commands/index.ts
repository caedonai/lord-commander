/**
 * Command management utilities
 * 
 * These modules handle command discovery, registration, shell completion,
 * and conflict resolution for CLI applications.
 */

// Export command registration and discovery
export * from './registerCommands.js';

// Export shell autocomplete functionality
export * from './autocomplete.js';